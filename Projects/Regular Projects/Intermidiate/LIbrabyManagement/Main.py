
# this is the main file will run the Library application
from MainFrame import App
if __name__ == "__main__":

    # if we are running the min application throught this application then we will run the application.

    app = App()
    app.mainloop()