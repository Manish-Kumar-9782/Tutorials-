#include<stdio.h>

// in this section we will see relational operator

void main(){
	
	// ==, <=, >=, +=, -=, *=, /=, %= operators are available in c programming language
	
	int a, b;
	
	a=10;  // assignment operator is used here , this is the mostly used operator in any language.
	b=5;
	// 0 is used for False value
	// 1 is used for True value
	
	printf(" == operator : %d\n", a==b);
	
	printf("<= operator : %d \n", a<=b);
	
	printf(">= operator : %d \n", a>=b);
	
	printf("+= operator : %d \n", a+=b);
	printf("Value of a after += ass")
	
	printf("-= operator : %d \n", a-=b);
	
	printf("*= operator : %d \n", a*=b);
	
	printf("/= operator : %d \n", a/=b);
	
	printf("%= operator : %d \n", a%=b);
}
